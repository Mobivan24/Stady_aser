node_modules/
logs/*.log
npm-debug.log*

# Локальные рабочие конфиги (UID меток, личные URL) — не коммитить.
# В репозитории хранятся только шаблоны config/*.example.json.
config/settings.json
config/allowed-tags.json
