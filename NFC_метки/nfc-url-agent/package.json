{
  "name": "nfc-url-agent",
  "version": "1.0.0",
  "private": true,
  "description": "Локальный NFC-агент для Windows: чтение NDEF URL с ACR122U и открытие только разрешённых ссылок из локальной таблицы допуска.",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test tests/*.test.js",
    "validate-config": "node scripts/validate-config.js"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "pcsclite": "^1.0.1"
  }
}
